const {LinkedList} = require('./linkedList');

describe('LinkedList', () =>{
    let list;

    beforeEach(() =>{
        list = new LinkedList();
    });

    test('that a linked list created id empty',() => {
        expect(list.head).toBeNull();
        expect
    })
})